PROGRAM specialextest;
VAR
    i, j, k, m : real;

BEGIN
    i := 1;
    j := 0;
    k := 4;
    WRITELN(SQRT(k));
    WRITELN(SIN(i));
    WRITELN(COS(j));
    WRITELN(LN(i));
    WRITELN(EXP(k));
    READLN(m);
    WRITELN(SQRT(m));
END.